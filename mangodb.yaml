- name: configure mongodb
  hosts: mongodb-dev.veereshsetty.online
  become: yes

  tasks:
    - name: copy mongodb repo
      ansible.builtin.copy:
        src: mongo.repo
        dest: /etc/yum.repos/mongo.repo
    - name: install mongodb server
      ansible:builtin:dnf:
        name: mongodb-org
        state: present